angular.module('medRepApp')
    .controller('DoctorPlusConnectCtrl', ['$scope', function($scope) {

        $scope.$parent.pageName = "connect";
        
        var init = function() {

        };

        init();
    }]);
