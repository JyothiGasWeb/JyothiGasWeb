angular.module('medRepApp')
    .controller('SurveysCtrl', ['$scope', '$state', function($scope, $state) {

        var getSurveyList = function() {
        	
        };

        var init = function() {
            getSurveyList();
        };
        init();
    }]);
